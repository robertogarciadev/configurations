server:
  port: 8761

spring:
  application:
    name: msvc-eureka

eureka:
  client:
    register-with-eureka: false
    fetch-register: false
    server-url:
      defaultZone: http://localhost:8761/eureka
